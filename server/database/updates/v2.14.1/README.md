# v2.14.1 升级说明

## 版本内容

- 后台菜单与权限展示名由「历史权益复核」统一为「权益复核」。
- 权益复核页 UI 对齐会员管理其它列表页（页头 / KPI / 过滤栏 / 表格卡片）。

## 数据库变更

- `menus` id=978 标题更新为「权益复核」。
- `permissions` `member.reward_review.list` 标题与描述同步更新。

## 升级步骤

1. 备份数据库。
2. 执行 `php think yd:update`（脚本幂等，可重复执行）。
3. 管理员重新登录后台以刷新菜单缓存。
